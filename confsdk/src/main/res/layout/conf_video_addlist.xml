<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/conf_video_addlist1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/app_main_hint"
    android:clickable="true"
    android:visibility="gone">

    <TextView
        style="@style/styleActionBar"
        android:background="@color/app_inconf_hue"
        android:text="@string/shareVideo"
        android:textColor="@color/white"
        android:textSize="@dimen/textSize_07"
        android:gravity="center" />

    <ListView
        android:id="@+id/lvShareVideo"
        style="@style/ff"
        android:divider="@color/a6_cuttingline3"
        android:dividerHeight="1px" />
</LinearLayout>